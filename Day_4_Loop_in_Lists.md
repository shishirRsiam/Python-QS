
# ✅ **20 Python List Problems (Using Loops)**

### *(You must use: list + for loop + print)*

---

## 🔹 **BASIC LEVEL (1–8)**

### **Q1.**

Create a list `nums = [1, 2, 3, 4]`.
Use a loop to print each element.

---

### **Q2.**

Create `values = [5, 10, 15]`.
Use a loop to print the sum of all elements.
**Expected:**

```
Total sum is: 30
```

---

### **Q3.**

Use `marks = [70, 80, 90, 88, 74, 83]`.
Print each mark with a message like:

```
Mark: 70
Mark: 80
Mark: 90
Mark: 88
Mark: 74
Mark: 74
Mark: 83
```

---

### **Q4.**

Create `numbers = [2, 4, 6, 8]`.
Use a loop to print the square of each number.

---

### **Q5.**

Create `names = ["A", "B", "C"]`.
Print all names using a loop.

---

### **Q6.**

Use `temps = [30, 32, 31]`.
Print the sum of temperatures using a loop.

---

### **Q7.**

Create `points = [5, 10, 15, 20]`.
Use a loop to count how many items are in the list without using `len()`.

---

### **Q8.**

Use `data = [3, 9, 27]`.
Print each value multiplied by 2.

---

---

## 🔹 **MODERATE LEVEL (9–14)**

### **Q9.**

Create `nums = [4, 12, 8, 16]`.
Print the largest number using a loop (no `max()` allowed).

---

### **Q10.**

Create `values = [7, 14, 21, 28]`.
Use a loop to print only elements greater than 10.

---

### **Q11.**

Use `marks = [55, 72, 88, 90]`.
Count how many marks are above 70 using a loop.

---

### **Q12.**

Create `ages = [12, 18, 25, 30]`.
Print the total of only the even ages.

---

### **Q13.**

Use `numbers = [1, 5, 2, 7, 3]`.
Print the smallest number using a loop (no `min()`).

---

### **Q14.**

Create `arr = [10, 20, 30, 40]`.
Use a loop to print the cumulative sum:

```
10
30
60
100
```

---

---

## 🔹 **INTERMEDIATE LEVEL (15–20)**

### **Q15.**

Use `nums = [3, 6, 9, 12]`.
Print the list in reverse order using a loop (no reverse(), no slicing).

---

### **Q16.**

Create `values = [1, 4, 9, 16]`.
Use a loop to print the average of all numbers.

---

### **Q17.**

Use `distances = [5, 10, 15, 20]`.
Print the total distance traveled.

---

### **Q18.**

Create `money = [100, 200, 300]`.
Use a loop to print each value with 10% added.
Example:

```
110
220
330
```

---

### **Q19.**

Use `result = []`
and
`nums = [1, 2, 3, 4]`.
Use a loop to append the square of each number into `result`.
Print result.

---

### **Q20.**

Create `values = [2, 3, 4]`.
Use a loop to compute factorial using list values:
**Expected:**

```
Factorial is: 24
```
